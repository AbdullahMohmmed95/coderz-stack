# ── System Resource Checks ────────────────────────────────────────────────────

define service {
    host_name               coderz-server
    service_description     CPU Load
    check_command           check_load_host!5.0,4.0,3.0!10.0,8.0,6.0
    check_interval          1
    retry_interval          1
    max_check_attempts      3
    check_period            24x7
    notification_interval   30
    notification_period     24x7
    contact_groups          admins
}

define service {
    host_name               coderz-server
    service_description     Disk Usage /
    check_command           check_disk_host!20%!10%!/hostfs/root
    check_interval          5
    retry_interval          1
    max_check_attempts      3
    check_period            24x7
    notification_interval   60
    notification_period     24x7
    contact_groups          admins
}

define service {
    host_name               coderz-server
    service_description     Memory Usage
    check_command           check_mem!85!95
    check_interval          1
    retry_interval          1
    max_check_attempts      3
    check_period            24x7
    notification_interval   30
    notification_period     24x7
    contact_groups          admins
}

define service {
    host_name               coderz-server
    service_description     Swap Usage
    check_command           check_swap!20%!10%
    check_interval          5
    retry_interval          1
    max_check_attempts      3
    check_period            24x7
    notification_interval   60
    notification_period     24x7
    contact_groups          admins
}

define service {
    host_name               coderz-server
    service_description     Total Processes
    check_command           check_procs!400!600
    check_interval          5
    retry_interval          1
    max_check_attempts      3
    check_period            24x7
    notification_interval   60
    notification_period     24x7
    contact_groups          admins
}

# ── HTTP Service Checks ───────────────────────────────────────────────────────

define service {
    host_name               coderz-server
    service_description     HTTP - Grafana
    check_command           check_http_port!3000!/login
    check_interval          1
    retry_interval          1
    max_check_attempts      3
    check_period            24x7
    notification_interval   10
    notification_period     24x7
    contact_groups          admins
}

define service {
    host_name               coderz-server
    service_description     HTTP - Prometheus
    check_command           check_http_port!9090!/-/healthy
    check_interval          1
    retry_interval          1
    max_check_attempts      3
    check_period            24x7
    notification_interval   10
    notification_period     24x7
    contact_groups          admins
}

define service {
    host_name               coderz-server
    service_description     HTTP - .NET API
    check_command           check_http_port!5050!/api/health
    check_interval          1
    retry_interval          1
    max_check_attempts      3
    check_period            24x7
    notification_interval   5
    notification_period     24x7
    contact_groups          admins
}

define service {
    host_name               coderz-server
    service_description     HTTP - WebApp (8888)
    check_command           check_http_port!8888!/api/health
    check_interval          1
    retry_interval          1
    max_check_attempts      3
    check_period            24x7
    notification_interval   5
    notification_period     24x7
    contact_groups          admins
}

define service {
    host_name               coderz-server
    service_description     HTTP - Kibana
    check_command           check_http_port!5601!/api/status
    check_interval          2
    retry_interval          1
    max_check_attempts      3
    check_period            24x7
    notification_interval   10
    notification_period     24x7
    contact_groups          admins
}

define service {
    host_name               coderz-server
    service_description     HTTP - Prefect UI
    check_command           check_http_port!4200!/api/health
    check_interval          2
    retry_interval          1
    max_check_attempts      3
    check_period            24x7
    notification_interval   10
    notification_period     24x7
    contact_groups          admins
}

define service {
    host_name               coderz-server
    service_description     HTTP - pgAdmin
    check_command           check_http_port!5080!/
    check_interval          5
    retry_interval          1
    max_check_attempts      3
    check_period            24x7
    notification_interval   30
    notification_period     24x7
    contact_groups          admins
}

define service {
    host_name               coderz-server
    service_description     HTTP - Nginx Landing
    check_command           check_http_port!80!/
    check_interval          1
    retry_interval          1
    max_check_attempts      3
    check_period            24x7
    notification_interval   5
    notification_period     24x7
    contact_groups          admins
}

define service {
    host_name               coderz-server
    service_description     HTTP - k6 Runner
    check_command           check_http_port!9000!/health
    check_interval          5
    retry_interval          1
    max_check_attempts      3
    check_period            24x7
    notification_interval   30
    notification_period     24x7
    contact_groups          admins
}

# ── TCP Port Checks ───────────────────────────────────────────────────────────

define service {
    host_name               coderz-server
    service_description     TCP - PostgreSQL (5433)
    check_command           check_tcp_port!5433
    check_interval          2
    retry_interval          1
    max_check_attempts      3
    check_period            24x7
    notification_interval   10
    notification_period     24x7
    contact_groups          admins
}

define service {
    host_name               coderz-server
    service_description     TCP - Elasticsearch (9200)
    check_command           check_tcp_port!9200
    check_interval          2
    retry_interval          1
    max_check_attempts      3
    check_period            24x7
    notification_interval   10
    notification_period     24x7
    contact_groups          admins
}

define service {
    host_name               coderz-server
    service_description     TCP - Loki (3100)
    check_command           check_tcp_port!3100
    check_interval          2
    retry_interval          1
    max_check_attempts      3
    check_period            24x7
    notification_interval   10
    notification_period     24x7
    contact_groups          admins
}

define service {
    host_name               coderz-server
    service_description     TCP - Redis (6379)
    check_command           check_tcp_port!6379
    check_interval          2
    retry_interval          1
    max_check_attempts      3
    check_period            24x7
    notification_interval   10
    notification_period     24x7
    contact_groups          admins
}
